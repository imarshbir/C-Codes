void swap( int *n1, int *n2 ){
   
    int temp=*n1;
    *n1=*n2;
    *n2=temp;
    
}

int main(){
    int a,b;
    printf ("Entre two no. to be swaped - ");
    scanf("%d %d",&a , &b);
    
    swap(&a,&b);
    printf(" \n Swaped No. are %d and %d \n ", a,b );
} 