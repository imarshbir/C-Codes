#include <stdio.h>

int main() {
    int a[] = {9, 83, 4, 5, 6, 6, 7};
    int max = a[0];

    for(int i = 1; i < 7; i++) {
        if(a[i] > max) {
            max = a[i];
        }
    }

    printf("%d", max);

    return 0;
}